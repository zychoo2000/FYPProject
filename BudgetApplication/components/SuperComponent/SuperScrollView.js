import React from 'react';
import {Text, View, TextInput, Pressable, ScrollView} from 'react-native';

const SuperScrollView = () => {
  <ScrollView>
    <View></View>
  </ScrollView>;
};
export default SuperScrollView;
